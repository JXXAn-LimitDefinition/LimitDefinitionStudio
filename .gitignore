# build output
dist/
# astro generated types
.astro/

# dependencies
node_modules/

# pnpm workspace store
.pnpm-store/

# local pnpm config (sandbox workaround; machine-specific absolute path)
.npmrc

# project meta / env
.env
.env.*

# local editor data (keep on your machine, not shipped)
admin/publish-log.json

# dsh project requirements (local only, not shipped)
.dsh/

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# os
.DS_Store
Thumbs.db
